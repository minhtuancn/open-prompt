# v1.0.0 Public Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Chuẩn bị Open Prompt cho public release: CI/CD hoàn chỉnh, documentation site, platform installers, onboarding wizard, và community prompts marketplace.

**Architecture:** 6 subsystems độc lập: (1) Release workflow với GitHub Actions + auto-publish artifacts, (2) VitePress documentation site, (3) Platform-specific installers config, (4) Onboarding wizard cho first-run experience, (5) Community prompts marketplace API + UI, (6) Telemetry opt-in.

**Tech Stack:** GitHub Actions, VitePress, Tauri bundler (NSIS/DMG/AppImage), React/TypeScript, Go (API handlers), SQLite

---

## File Structure

### Release workflow
- Modify: `.github/workflows/build-release.yml` — thêm auto-publish, update manifest, signing
- Create: `.github/workflows/update-manifest.yml` — tạo update-manifest.json cho auto-updater
- Modify: `scripts/release.sh` — thêm version bump automation

### Documentation site
- Create: `docs/site/package.json` — VitePress project
- Create: `docs/site/.vitepress/config.ts` — VitePress config
- Create: `docs/site/index.md` — Landing page
- Create: `docs/site/guide/getting-started.md` — Hướng dẫn cài đặt
- Create: `docs/site/guide/providers.md` — Cấu hình providers
- Create: `docs/site/guide/hotkeys.md` — Phím tắt
- Create: `docs/site/guide/plugins.md` — Plugin system
- Create: `docs/site/api/engine-rpc.md` — Go Engine JSON-RPC API reference

### Platform installers
- Modify: `src-tauri/tauri.conf.json` — NSIS config (Windows), DMG (macOS), AppImage/deb (Linux)

### Onboarding wizard
- Create: `src/components/onboarding/OnboardingWizard.tsx` — Wizard container với steps
- Create: `src/components/onboarding/WelcomeStep.tsx` — Trang chào mừng
- Create: `src/components/onboarding/ProviderStep.tsx` — Chọn/kết nối provider
- Create: `src/components/onboarding/HotkeyStep.tsx` — Chọn hotkey
- Create: `src/components/onboarding/DoneStep.tsx` — Hoàn tất
- Modify: `src/App.tsx` — Show wizard khi first_run = true

### Community prompts marketplace
- Create: `go-engine/db/migrations/006_marketplace.sql` — shared_prompts table
- Create: `go-engine/db/repos/marketplace_repo.go` — CRUD shared prompts
- Create: `go-engine/api/handlers_marketplace.go` — marketplace.list/publish/install/search
- Modify: `go-engine/api/router.go` — register marketplace routes
- Modify: `go-engine/db/sqlite.go` — embed migration 006
- Create: `src/components/settings/MarketplaceTab.tsx` — Browse + install community prompts
- Modify: `src/components/settings/SettingsLayout.tsx` — thêm Marketplace tab

### Telemetry
- Create: `go-engine/telemetry/telemetry.go` — opt-in telemetry client
- Create: `go-engine/api/handlers_telemetry.go` — telemetry.opt_in, telemetry.status
- Modify: `go-engine/api/router.go` — register telemetry routes

---

## Task 1: Release workflow — auto-publish artifacts

**Files:**
- Modify: `.github/workflows/build-release.yml`
- Create: `.github/workflows/update-manifest.yml`

- [ ] **Step 1: Cập nhật build-release.yml — thêm artifact upload**

```yaml
# Thêm vào job build, sau bước Build Tauri app:
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: open-prompt-${{ matrix.target }}
          path: |
            src-tauri/target/${{ matrix.target }}/release/bundle/**/*.deb
            src-tauri/target/${{ matrix.target }}/release/bundle/**/*.AppImage
            src-tauri/target/${{ matrix.target }}/release/bundle/**/*.dmg
            src-tauri/target/${{ matrix.target }}/release/bundle/**/*.exe
            src-tauri/target/${{ matrix.target }}/release/bundle/**/*.msi
```

- [ ] **Step 2: Tạo update-manifest workflow**

```yaml
# .github/workflows/update-manifest.yml
name: Generate Update Manifest

on:
  release:
    types: [published]

jobs:
  manifest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Generate update manifest
        run: |
          VERSION="${{ github.event.release.tag_name }}"
          cat > update-manifest.json << EOF
          {
            "version": "${VERSION#v}",
            "notes": "${{ github.event.release.body }}",
            "pub_date": "$(date -u +%FT%TZ)",
            "platforms": {
              "linux-x86_64": {
                "url": "https://github.com/minhtuancn/open-prompt/releases/download/$VERSION/open-prompt_${VERSION#v}_amd64.AppImage.tar.gz",
                "signature": ""
              },
              "darwin-x86_64": {
                "url": "https://github.com/minhtuancn/open-prompt/releases/download/$VERSION/open-prompt_${VERSION#v}_x64.app.tar.gz",
                "signature": ""
              },
              "darwin-aarch64": {
                "url": "https://github.com/minhtuancn/open-prompt/releases/download/$VERSION/open-prompt_${VERSION#v}_aarch64.app.tar.gz",
                "signature": ""
              },
              "windows-x86_64": {
                "url": "https://github.com/minhtuancn/open-prompt/releases/download/$VERSION/open-prompt_${VERSION#v}_x64-setup.nsis.zip",
                "signature": ""
              }
            }
          }
          EOF
      - name: Upload manifest to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ github.event.release.tag_name }} update-manifest.json --clobber
```

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/build-release.yml .github/workflows/update-manifest.yml
git commit -m "feat: thêm auto-publish artifacts + update manifest workflow"
```

---

## Task 2: Platform installers config

**Files:**
- Modify: `src-tauri/tauri.conf.json`

- [ ] **Step 1: Thêm NSIS + DMG + deb config**

Thêm vào `bundle` object trong tauri.conf.json:

```json
{
  "bundle": {
    "active": true,
    "targets": ["nsis", "dmg", "deb", "appimage"],
    "icon": ["icons/32x32.png", "icons/128x128.png", "icons/128x128@2x.png", "icons/icon.icns", "icons/icon.ico"],
    "externalBin": ["binaries/go-engine"],
    "macOS": {
      "signingIdentity": null,
      "entitlements": null,
      "minimumSystemVersion": "10.15"
    },
    "windows": {
      "certificateThumbprint": null,
      "digestAlgorithm": "sha256",
      "timestampUrl": "http://timestamp.digicert.com",
      "nsis": {
        "displayLanguageSelector": true,
        "languages": ["English", "Vietnamese"],
        "installerIcon": "icons/icon.ico",
        "headerImage": "icons/icon.png"
      }
    },
    "linux": {
      "deb": {
        "depends": ["libwebkit2gtk-4.1-0", "libgtk-3-0", "libayatana-appindicator3-1", "xclip"],
        "section": "utils"
      },
      "appimage": {
        "bundleMediaFramework": false
      }
    }
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add src-tauri/tauri.conf.json
git commit -m "feat: cấu hình installers cho Windows (NSIS), macOS (DMG), Linux (deb/AppImage)"
```

---

## Task 3: Documentation site — VitePress

**Files:**
- Create: `docs/site/package.json`
- Create: `docs/site/.vitepress/config.ts`
- Create: `docs/site/index.md`
- Create: `docs/site/guide/getting-started.md`
- Create: `docs/site/guide/providers.md`
- Create: `docs/site/guide/hotkeys.md`
- Create: `docs/site/guide/plugins.md`
- Create: `docs/site/api/engine-rpc.md`

- [ ] **Step 1: Tạo VitePress project**

```bash
mkdir -p docs/site && cd docs/site
npm init -y
npm install -D vitepress
```

- [ ] **Step 2: Tạo VitePress config**

```typescript
// docs/site/.vitepress/config.ts
import { defineConfig } from 'vitepress'

export default defineConfig({
  title: 'Open Prompt',
  description: 'Desktop AI assistant — đa provider, đa ngôn ngữ',
  lang: 'vi-VN',
  themeConfig: {
    nav: [
      { text: 'Hướng dẫn', link: '/guide/getting-started' },
      { text: 'API', link: '/api/engine-rpc' },
      { text: 'GitHub', link: 'https://github.com/minhtuancn/open-prompt' },
    ],
    sidebar: [
      {
        text: 'Hướng dẫn',
        items: [
          { text: 'Bắt đầu', link: '/guide/getting-started' },
          { text: 'Providers', link: '/guide/providers' },
          { text: 'Phím tắt', link: '/guide/hotkeys' },
          { text: 'Plugins', link: '/guide/plugins' },
        ],
      },
      {
        text: 'API Reference',
        items: [
          { text: 'Engine RPC', link: '/api/engine-rpc' },
        ],
      },
    ],
    footer: {
      message: 'Released under MIT License',
    },
  },
})
```

- [ ] **Step 3: Tạo landing page + guide pages**

Tạo `docs/site/index.md` (landing), `guide/getting-started.md`, `guide/providers.md`, `guide/hotkeys.md`, `guide/plugins.md`, `api/engine-rpc.md` với nội dung từ README + CHANGELOG.

- [ ] **Step 4: Thêm build script**

```json
// Thêm vào docs/site/package.json scripts
"docs:dev": "vitepress dev",
"docs:build": "vitepress build",
"docs:preview": "vitepress preview"
```

- [ ] **Step 5: Commit**

```bash
git add docs/site/
git commit -m "docs: tạo documentation site với VitePress"
```

---

## Task 4: Onboarding wizard

**Files:**
- Create: `src/components/onboarding/OnboardingWizard.tsx`
- Create: `src/components/onboarding/WelcomeStep.tsx`
- Create: `src/components/onboarding/ProviderStep.tsx`
- Create: `src/components/onboarding/HotkeyStep.tsx`
- Create: `src/components/onboarding/DoneStep.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: Tạo WelcomeStep**

```tsx
// src/components/onboarding/WelcomeStep.tsx
interface Props { onNext: () => void }

export function WelcomeStep({ onNext }: Props) {
  return (
    <div className="flex flex-col items-center gap-6 py-8">
      <div className="text-4xl">✨</div>
      <h1 className="text-xl font-bold text-white">Chào mừng đến Open Prompt</h1>
      <p className="text-sm text-white/60 text-center max-w-md">
        Desktop AI assistant hỗ trợ đa provider. Nhấn hotkey bất cứ lúc nào để gọi AI.
      </p>
      <button onClick={onNext}
        className="px-6 py-2 bg-indigo-500 hover:bg-indigo-400 text-white rounded-lg text-sm">
        Bắt đầu thiết lập
      </button>
    </div>
  )
}
```

- [ ] **Step 2: Tạo ProviderStep — chọn provider + nhập API key**

```tsx
// src/components/onboarding/ProviderStep.tsx
// Hiện danh sách providers (Anthropic, OpenAI, Gemini, Ollama)
// Input API key cho provider được chọn
// Nút "Bỏ qua" + "Tiếp tục"
```

- [ ] **Step 3: Tạo HotkeyStep — chọn hotkey**

```tsx
// src/components/onboarding/HotkeyStep.tsx
// 6 preset options (giống HotkeyTab)
// Lưu ngay khi chọn
```

- [ ] **Step 4: Tạo DoneStep**

```tsx
// src/components/onboarding/DoneStep.tsx
// Thông báo hoàn tất
// Nhấn hotkey lần đầu để thử
```

- [ ] **Step 5: Tạo OnboardingWizard container**

```tsx
// src/components/onboarding/OnboardingWizard.tsx
import { useState } from 'react'
import { WelcomeStep } from './WelcomeStep'
import { ProviderStep } from './ProviderStep'
import { HotkeyStep } from './HotkeyStep'
import { DoneStep } from './DoneStep'

interface Props { onComplete: () => void }

export function OnboardingWizard({ onComplete }: Props) {
  const [step, setStep] = useState(0)
  const steps = [
    <WelcomeStep onNext={() => setStep(1)} />,
    <ProviderStep onNext={() => setStep(2)} onSkip={() => setStep(2)} />,
    <HotkeyStep onNext={() => setStep(3)} />,
    <DoneStep onComplete={onComplete} />,
  ]
  return (
    <div className="fixed inset-0 bg-black/90 backdrop-blur flex items-center justify-center z-50">
      <div className="bg-[#1a1a2e] border border-white/10 rounded-2xl p-8 w-[480px] max-h-[90vh] overflow-y-auto">
        {/* Progress dots */}
        <div className="flex justify-center gap-2 mb-6">
          {[0,1,2,3].map(i => (
            <div key={i} className={`w-2 h-2 rounded-full ${i === step ? 'bg-indigo-500' : 'bg-white/20'}`} />
          ))}
        </div>
        {steps[step]}
      </div>
    </div>
  )
}
```

- [ ] **Step 6: Integrate vào App.tsx — check auth.is_first_run**

```tsx
// Trong App.tsx, thêm:
const [showOnboarding, setShowOnboarding] = useState(false)

useEffect(() => {
  callEngine<boolean>('auth.is_first_run', {}).then(isFirst => {
    if (isFirst) setShowOnboarding(true)
  })
}, [])

// Render:
{showOnboarding && <OnboardingWizard onComplete={() => setShowOnboarding(false)} />}
```

- [ ] **Step 7: Run TypeScript check**

```bash
npx tsc --noEmit
```

- [ ] **Step 8: Commit**

```bash
git add src/components/onboarding/ src/App.tsx
git commit -m "feat: thêm onboarding wizard cho first-run experience"
```

---

## Task 5: Community prompts marketplace — Backend

**Files:**
- Create: `go-engine/db/migrations/006_marketplace.sql`
- Create: `go-engine/db/repos/marketplace_repo.go`
- Create: `go-engine/api/handlers_marketplace.go`
- Modify: `go-engine/api/router.go`
- Modify: `go-engine/db/sqlite.go`

- [ ] **Step 1: Tạo migration 006**

```sql
-- go-engine/db/migrations/006_marketplace.sql
CREATE TABLE IF NOT EXISTS shared_prompts (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id     INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    title       TEXT    NOT NULL,
    content     TEXT    NOT NULL,
    description TEXT,
    category    TEXT,
    tags        TEXT,
    downloads   INTEGER NOT NULL DEFAULT 0,
    is_public   INTEGER NOT NULL DEFAULT 1,
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_shared_prompts_category ON shared_prompts(category);
CREATE INDEX IF NOT EXISTS idx_shared_prompts_downloads ON shared_prompts(downloads DESC);
```

- [ ] **Step 2: Tạo MarketplaceRepo**

```go
// go-engine/db/repos/marketplace_repo.go
package repos

type SharedPrompt struct {
    ID          int64  `json:"id"`
    UserID      int64  `json:"user_id"`
    Title       string `json:"title"`
    Content     string `json:"content"`
    Description string `json:"description"`
    Category    string `json:"category"`
    Tags        string `json:"tags"`
    Downloads   int    `json:"downloads"`
    IsPublic    bool   `json:"is_public"`
    CreatedAt   string `json:"created_at"`
}

// Methods: List, Search, Publish, Install (copy to user prompts), IncrDownloads
```

- [ ] **Step 3: Tạo API handlers**

```go
// go-engine/api/handlers_marketplace.go
// marketplace.list — danh sách public prompts (pagination + sort by downloads)
// marketplace.search — tìm theo keyword
// marketplace.publish — user publish prompt ra marketplace
// marketplace.install — copy shared prompt vào user's prompts + increment downloads
```

- [ ] **Step 4: Register routes trong router.go + embed migration trong sqlite.go**

- [ ] **Step 5: Run Go tests**

```bash
cd go-engine && go test ./... -count=1
```

- [ ] **Step 6: Commit**

```bash
git add go-engine/db/migrations/006_marketplace.sql go-engine/db/repos/marketplace_repo.go \
  go-engine/api/handlers_marketplace.go go-engine/api/router.go go-engine/db/sqlite.go
git commit -m "feat: thêm community prompts marketplace — backend API + migration"
```

---

## Task 6: Community prompts marketplace — Frontend

**Files:**
- Create: `src/components/settings/MarketplaceTab.tsx`
- Modify: `src/components/settings/SettingsLayout.tsx`

- [ ] **Step 1: Tạo MarketplaceTab**

```tsx
// src/components/settings/MarketplaceTab.tsx
// Hiện danh sách shared prompts
// Search bar
// Install button → callEngine('marketplace.install', ...)
// Publish button → callEngine('marketplace.publish', ...)
// Sort by: downloads, newest
```

- [ ] **Step 2: Thêm tab Marketplace vào SettingsLayout**

- [ ] **Step 3: Run TypeScript check**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add src/components/settings/MarketplaceTab.tsx src/components/settings/SettingsLayout.tsx
git commit -m "feat: thêm Marketplace tab — browse + install community prompts"
```

---

## Task 7: Telemetry opt-in

**Files:**
- Create: `go-engine/telemetry/telemetry.go`
- Create: `go-engine/api/handlers_telemetry.go`
- Modify: `go-engine/api/router.go`

- [ ] **Step 1: Tạo telemetry package**

```go
// go-engine/telemetry/telemetry.go
package telemetry

// Client ghi nhận events cơ bản (app_start, query_count, provider_used)
// Chỉ gửi khi user opt-in qua settings
// Không gửi nội dung query hay response
type Client struct {
    enabled  bool
    endpoint string
}

func New(enabled bool) *Client { ... }
func (c *Client) Track(event string, props map[string]string) { ... }
```

- [ ] **Step 2: Tạo API handlers**

```go
// telemetry.opt_in — toggle telemetry on/off (lưu vào settings)
// telemetry.status — trả về enabled/disabled
```

- [ ] **Step 3: Register routes**

- [ ] **Step 4: Run Go tests**

```bash
cd go-engine && go test ./... -count=1
```

- [ ] **Step 5: Commit**

```bash
git add go-engine/telemetry/ go-engine/api/handlers_telemetry.go go-engine/api/router.go
git commit -m "feat: thêm telemetry opt-in — track events cơ bản khi user đồng ý"
```

---

## Task 8: Cập nhật docs + CHANGELOG + final commit

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `ROADMAP.md`
- Modify: `README.md`
- Modify: `package.json` — bump version to 1.0.0
- Modify: `src-tauri/tauri.conf.json` — bump version to 1.0.0

- [ ] **Step 1: Cập nhật CHANGELOG cho v1.0.0**

- [ ] **Step 2: Tick ROADMAP v1.0.0 items**

- [ ] **Step 3: Bump version trong package.json và tauri.conf.json**

- [ ] **Step 4: Cập nhật README.md — thêm link docs site, installation instructions**

- [ ] **Step 5: Commit + tag**

```bash
git add CHANGELOG.md ROADMAP.md README.md package.json src-tauri/tauri.conf.json
git commit -m "docs: cập nhật tài liệu v1.0.0 Public Release"
git tag v1.0.0
```

- [ ] **Step 6: Push**

```bash
git push origin main --tags
```
